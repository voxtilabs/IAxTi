packages:
  - "apps/*"
  - "packages/*"
  - "packages/modules/*"

# pnpm 12 bloquea los build scripts de dependencias salvo lista explícita.
allowBuilds:
  esbuild: true
  "@scarf/scarf": false # telemetría que arrastra @nestjs/swagger: bloqueada a propósito
