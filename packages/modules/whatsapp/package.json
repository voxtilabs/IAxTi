{
  "name": "@iaxti/module-whatsapp",
  "version": "0.1.0",
  "private": true,
  "main": "dist/contract.js",
  "types": "dist/contract.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@iaxti/core": "workspace:*",
    "@iaxti/module-channels": "workspace:*",
    "ioredis": "^5.4.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@iaxti/db": "workspace:*",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
