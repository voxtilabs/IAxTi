module:
  id: identity
  version: 0.1.0
  core: true
depends_on:
  required: []
  optional: []
permissions:
  - users.read
  - users.invite
  - users.manage
events:
  publishes:
    - user.registered
    - user.invited
    - user.joined_tenant
    - user.login_failed
  consumes: []
tools: []
# La API de equipo está desde #28 y no tenía pantalla — y "Invita a tu
# equipo" es un paso del onboarding que nadie podía completar desde la app.
nav:
  - { label: Equipo, path: /ajustes/equipo, permission: users.read, grupo: Configuración }
widgets: []
plan_min: base
flag: module.identity
